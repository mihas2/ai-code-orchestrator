# AI Code Orchestrator Development Progress

## Current focus

- Stabilize orchestration planning, implementation, and review workflows.
- Keep the VS Code extension, webview, CLI, and shared packages aligned.
- Preserve compatibility with existing integrations while migrating user-facing branding.

## Validation

- Run `pnpm lint` for repository linting.
- Run `pnpm check-types` for TypeScript validation.
- Run `pnpm test` for the workspace test suite.
