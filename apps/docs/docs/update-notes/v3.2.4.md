---
description: AI Code Orchestrator 3.2.4 ensures the apply_diff tool is only used when diff editing (Fast Edits) is enabled in settings for proper tool usage.
keywords:
    - AI Code Orchestrator
    - v3.2.4
    - diff tool
    - fast edits
    - apply_diff
    - settings alignment
    - tool usage
    - release notes
---

# AI Code Orchestrator 3.2.4 Release Notes

This patch release ensures diff tool usage aligns with settings.

---

## Fixes

- Ensured the `apply_diff` tool is only used if diff editing ("Fast Edits") is enabled in settings.
